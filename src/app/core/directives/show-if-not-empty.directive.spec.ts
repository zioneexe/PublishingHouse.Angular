import { ShowIfNotEmptyDirective } from './show-if-not-empty.directive';

describe('ShowIfNotEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfNotEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
