import { CurrencyUAHPipe } from './currency-uah.pipe';

describe('CurrencyUAHPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyUAHPipe();
    expect(pipe).toBeTruthy();
  });
});
